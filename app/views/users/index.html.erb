<h2>All Projects</h2>
<ul class="users">
  <% @users.each do |user| %>
    <li>
        <ul class="projects">
        <% user.projects.each do |project| %>
          <li class="project_title">
            <%= link_to project.title, "/projects/#{project.id}" %>
          </li>
        <% end %>
        </ul>
      </p>
    </li>
  <% end %>
</ul>
